default_cfg = {
    pid_file  = "/var/run/tarantool",
    wal_dir   = "../tarantool_wal",
    memtx_dir = "../tarantool_memtx",
    vinyl_dir = "../tarantool_vinyl",
    log       = "../db_logs",
    username  = "../tarantool",
    language  = "Lua",
}
instance_dir = "."
